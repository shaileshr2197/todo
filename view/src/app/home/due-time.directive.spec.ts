import { DueTimeDirective } from './due-time.directive';

describe('DueTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new DueTimeDirective();
    expect(directive).toBeTruthy();
  });
});
